/target
Cargo.lock.bak
*.sqlite
*.db

# Claude Code per-user local settings (never shared)
.claude/settings.local.json

# stray local build artifact
/verify_fix

# Transient git worktrees created for isolated agent runs. They are real
# repositories, so a bare `git add -A` commits them as gitlinks.
.claude/worktrees/
